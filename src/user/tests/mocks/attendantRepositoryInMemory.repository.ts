// import { Attendant } from "src/user/entities";
// import { Repository } from "typeorm";

// export class AttendantRepositoryInMemory extends Repository<Attendant> {

//   attendants: Attendant[] = [];

//   async getAttendantByEmail(email: string): Promise<Attendant | null> {
//     return this.attendants.find(attendant => attendant.email === email);
//   }

// }